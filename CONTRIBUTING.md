# 프로젝트 기여 가이드 (How to Contribute)

저희 프로젝트에 관심을 가지고 기여해 주셔서 감사합니다! 모든 기여는 이 조직의 워크플로우를 따르며, 코드 리뷰를 통해 관리됩니다.

## 🤝 협업의 기본 원칙

* 모든 작업은 **GitHub Issues**에서 시작하는 것을 원칙으로 합니다.
* 모든 코드는 `dev` (또는 `develop`) 브랜치를 대상으로 **Pull Request(PR)**를 통해서만 병합됩니다.
* 모든 PR은 최소 1명 이상의 리뷰어에게 **승인(Approve)**을 받아야 합니다.

---

## 🐞 버그 리포트

버그를 발견하셨나요?

1.  먼저 **[Issues]** 탭에서 혹시 동일한 버그가 이미 보고되었는지 검색합니다.
2.  보고된 내용이 없다면, **'New Issue'** 버튼을 클릭하여 **'버그 보고(Bug Report)'** 템플릿을 선택하고 양식에 맞게 작성해 주세요. (특히 '재현 단계'를 상세히 적어주시는 것이 큰 도움이 됩니다!)

## ✨ 기능 제안

새로운 기능이나 개선 사항을 제안하고 싶으신가요?

1.  **[Issues]** 탭에서 **'New Issue'** 버튼을 클릭합니다.
2.  **'기능 요구사항(Feature)'** 을 선택하여 작성해 주세요.

---

## 🧑‍💻 코드 기여 워크플로우

코드를 직접 수정하여 기여하는 절차는 다음과 같습니다.

### 1. Issue 확인 및 브랜치 생성

1.  작업을 시작하기 전, 본인이 담당할 **Issue가 있는지 확인**합니다. (없다면 요청해주시기 바랍니다.)
2.  로컬 저장소의 `dev` 브랜치를 최신 상태로 업데이트합니다.
    ```bash
    git checkout dev
    git pull origin dev
    ```
3.  작업할 내용을 기반으로 **새로운 브랜치**를 생성합니다. 브랜치 이름은 Issue 번호와 내용을 포함하는 것을 권장합니다.
    ```bash
    # 예시: feature/123-login-api-validation
    git checkout -b <type>/<issue-number>-<description>
    ```

### 2. 코드 작업 및 커밋

1.  코드를 수정하고, `.editorconfig`에 정의된 코딩 스타일을 준수합니다.
2.  작업 내용을 **의미 있는 단위로 커밋(Commit)**합니다. (커밋 메시지는 이 작업이 '무엇'을 '왜' 했는지 명확히 드러나는 것이 좋습니다.)

### 3. Pull Request (PR) 생성

1.  작업이 완료되면, 로컬 브랜치를 GitHub 저장소로 푸시(Push)합니다.
    ```bash
    git push origin feature/123-login-api-validation
    ```
2.  GitHub 저장소에서 **`dev` 브랜치**를 대상으로 **새로운 Pull Request**를 생성합니다.
3.  PR 템플릿 양식에 맞춰 상세한 내용을 작성합니다.

### 4. 코드 리뷰

1.  PR이 생성되면 자동으로 리뷰어가 배정되거나, 적절한 동료에게 리뷰를 요청합니다.
2.  리뷰어가 변경을 요청(Changes Requested)하면, 피드백을 반영하여 코드를 수정하고 다시 푸시합니다.
3.  PR이 **승인(Approved)**되면, `dev` 브랜치에 병합(Merge)됩니다.